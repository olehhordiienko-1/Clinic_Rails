<h1>Specializations</h1>

<table class="table">
  <thead>
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @specializations.each do |specialization| %>
    <tr>
      <td><%= specialization.name %></td>
      <td><%= link_to 'Show', specialization %></td>
      <td><%= link_to 'Edit', edit_specialization_path(specialization) %></td>
      <td>
        <%= form_with(model: specialization, url: specialization_path(specialization), method: :delete, data: { confirm: 'Are you sure?' }) do |form| %>
          <%= form.submit 'Destroy' %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Specialization', new_specialization_path %>
